# ExperienceGraph: 基于经验图谱的 LLM 智能体增量学习框架

## 1. 研究荟萃分析与相关工作

### 1.1 LLM 智能体在游戏环境中的决策

| 工作 | 核心机制 | 局限 |
|------|---------|------|
| **ReAct** (Yao et al., 2023) | 交替推理+行动，无持久记忆 | 每次从零推理，不积累经验 |
| **Reflexion** (Shinn et al., 2023) | 自然语言反思存储为文本记忆 | 经验是扁平文本列表，无结构化关系 |
| **Voyager** (Wang et al., 2023) | 技能库 + 自动课程 | 技能是代码片段，不建模路径间关系和条件依赖 |
| **GITM** (Zhu et al., 2023) | 目标分解 + LLM 规划 | 固定分解逻辑，不基于历史经验动态调整 |
| **DEPS** (Wang et al., 2023) | 描述-解释-规划-选择 | 无跨 episode 学习能力 |

### 1.2 研究 Gap

现有方法的共同缺陷：
1. **无结构化经验积累**：要么无记忆(ReAct)，要么记忆是扁平的(Reflexion)，要么是孤立技能(Voyager)
2. **无条件路径建模**：不建模"在状态X下路径A优于路径B"这种上下文相关知识
3. **无增量收敛**：不随经验增加而系统性地提升决策质量

### 1.3 我们的定位

我们提出 **ExperienceGraph**——一个基于图结构的经验积累框架，使 LLM 智能体能够：
- 将每次任务执行的路径结构化为图
- 基于状态相似度合并经验
- 结合统计信号（成功率）和 LLM 判断进行路径选择
- 随游戏次数增加，决策质量单调递增

---

## 2. Hypotheses

### H1: 经验图谱提升任务成功率
> 配备 ExperienceGraph 的 LLM 智能体在相同任务上的成功率显著高于无经验积累的 baseline（ReAct），且随 episode 数增加差距扩大。

### H2: 图结构加速决策收敛
> ExperienceGraph 智能体达到稳定成功率所需的 episode 数显著少于扁平记忆方法（Reflexion-style），证明图结构的组织方式加速了经验利用。

### H3: 探索-利用平衡机制有效
> 每次决策前先生成新路径再对比已有路径的机制，能持续发现更优路径并整合进图谱，防止过早收敛到次优解。

---

## 3. 程序环境设计

### 3.1 TextCraft：文字版 Minecraft

一个简化的文字游戏环境，保留 Minecraft 的核心决策复杂度，去除视觉和操作复杂度。

**状态空间（约 10 维度）：**

```json
{
  "inventory": {
    "diamond": 0,
    "emerald": 3,
    "iron_ingot": 5,
    "wood": 10,
    "pickaxe": "iron",
    "crafting_table": true
  },
  "environment": {
    "nearby_village": true,
    "nearby_mine": false,
    "nearby_crafting_table": true,
    "biome": "plains"
  },
  "ambiguous": {
    "village_has_armorer": "unknown"
  }
}
```

**可执行动作集：**
- `mine(resource)` — 挖矿（需要对应工具 + 附近有矿）
- `craft(item)` — 合成（需要材料 + 工作台）
- `trade(villager, offer, want)` — 村民交易（需要在村庄 + 有对应物品）
- `move_to(location)` — 移动到某处
- `explore()` — 探索未知区域（可能发现新资源/村庄）
- `gather(resource)` — 采集地表资源

**目标任务：**
- 主线任务：获取完整钻石套装（头盔、胸甲、护腿、靴子）
- 需要 24 颗钻石（简化为：需要"足够的钻石"）
- 多条可行路径：挖矿合成 / 村民交易 / 混合策略

### 3.2 经验图谱定义

经验图谱 G = (V, E) 是一个有向加权图，随智能体的游戏经验增量构建。

**节点 V — 表示"状态条件集合"**

每个节点不是一个具体的游戏状态快照，而是一组进入条件（类似于"这个阶段需要什么"）。节点代表决策路径上的一个关键检查点。

```json
{
  "id": "v_003",
  "label": "持有足够钻石",
  "type": "checkpoint",
  "required": {"diamond": ">=24", "crafting_table": true},
  "suggested": "附近有工作台可以省去移动步骤",
  "stats": {"attempts": 30, "successes": 22}
}
```

节点类型：
- `goal`：最终目标节点（如"获得钻石套装"）
- `checkpoint`：中间检查点（如"持有足够钻石"）
- `observation`：由探索或检查动作揭示的信息节点（如"村庄有盔甲匠"）

**边 E — 表示"从一个检查点到另一个检查点的转移"**

每条边代表：满足当前节点条件后，执行某动作可以到达下一个节点。

```json
{
  "from": "v_001",
  "to": "v_003",
  "action": "mine(diamond)",
  "hard_precondition": {"pickaxe": "iron|diamond"},
  "soft_precondition": "附近有矿洞时效率高",
  "stats": {"attempts": 20, "successes": 14}
}
```

边的两层条件：
- **hard_precondition**：JSON 格式，代码硬检查。不满足则这条边不可用。
- **soft_precondition**：自然语言，供 LLM 参考。影响路径选择但不阻断。

**路径 P — 从起始节点到目标节点的有序边序列**

```
P = [e1, e2, ..., ek]  其中 e_i.to == e_{i+1}.from
```

路径优先使用整条路径的历史成功率；当路径级统计不足时，使用路径上各边成功率的乘积作为近似估计，并加入平滑项避免少量样本导致过度自信。展示给智能体的格式：

```
路径A: 挖矿→合成 (成功率 14/20, 平均7步)
路径B: 村民交易 (成功率 8/10, 平均4步)
路径C: [新生成, 无历史数据]
```

**节点合并算法**

当新路径整合进图谱时，需要判断新节点是否与已有节点"等价"：

1. **规范化**：先将条件改写为标准形式，例如 `diamond>=24`、`has_enough_diamond=true` 统一到同一字段
2. **规则判断**：若 `required` 条件集合完全相同，视为同一节点，直接合并统计数据
3. **包含或冲突判断**：若一个条件集合包含另一个，保留更具体节点；若条件冲突（如 `has_armorer=true` 与 `has_armorer=false`），禁止合并
4. **语义判断**：只有在规则无法判断、但两个节点存在语义重叠时，才调用 LLM 判断是否应该合并，并记录合并理由
5. **新建节点**：若 `required` 无交集或语义判断为不等价，则创建新节点

合并时：stats 累加，soft_precondition 取并集（去重）。

### 3.3 图增长复杂度分析与压缩机制

**核心问题：图谱会不会无限膨胀？**

老师的关键问题：随着 episode 增加，图的大小如何增长？需要说明它不会失控。

**增长上界分析：**

在 TextCraft 环境中，状态空间是有限且离散的：
- 材料种类有限（约 10 种）
- 每种材料的条件判断是离散的（充足/不足/存在/不存在）
- 环境维度有限（约 5 种）
- 动作集有限（6 种动作 × 有限参数组合）

因此节点数存在有限上界，约等于 required 条件的有效组合数。若将 10 个关键条件近似二值化，直观上界为 2^10 = 1024 个节点；实际实现中包含类别变量和阈值条件，因此会在实验中报告真实的 |V|、|E| 增长曲线，而不是依赖该粗略估计。

**预期增长模式：经验性亚线性**

- 早期（episode 1-50）：图快速增长，每轮可能新增 2-3 个节点
- 中期（episode 50-150）：增长放缓，大部分新路径命中已有节点，仅补充新边
- 后期（episode 150+）：趋近饱和，新增主要是统计数据更新，图结构基本稳定

我们不在方法部分声称严格的对数复杂度，而是在实验中检验图规模是否随 episode 增长逐渐放缓。关键指标包括 |V|、|E|、新增节点率、新增边率和每次展示给 LLM 的 token 数。

**主动压缩机制 — "经验蒸馏"：**

即便增长有自然上界，仍需压缩机制保证 prompt 可控：

1. **路径剪枝**：成功率低于阈值（如 < 10%）且尝试次数超过 K 次的边，标记为"休眠"，不再展示给智能体
2. **统计衰减**：每 50 轮对所有 stats 乘以衰减因子（如 0.9），让近期经验权重更高，远古失败路径自然淡出
3. **Top-K 展示**：每次决策时，只向智能体展示从当前状态可达的 Top-K 条路径（按成功率排序），而非整个图谱
4. **节点折叠**：当一个 checkpoint 节点的所有出边都指向同一个下游节点时，可以折叠为一条边

**内存常数保证：**

无论图谱实际大小如何，展示给 LLM 的信息量永远控制在固定 token 预算内（如 1000 tokens）。超出时触发摘要：保留 Top-K 路径完整描述 + 其余路径的一行统计摘要。

这类似于老师说的"不管积累多少经验，总结成 1000 字"——我们的图谱是完整保留的，但每次决策时的"视图"是固定大小的。

### 3.4 为什么设计 TextCraft — 环境的普适性价值

**为什么不直接用真实 Minecraft？**

1. **控制变量**：真实 MC 有视觉感知、物理引擎、随机事件等干扰因素，难以隔离"决策能力"这一个变量
2. **实验成本**：真实 MC 一轮游戏可能需要几十分钟，300 轮 × 5 条件 × 5 种子 = 不可行的时间成本
3. **可复现性**：文字环境完全确定性（给定种子），保证实验可复现

**TextCraft 作为 Benchmark 的独立价值**

TextCraft 不只是"简化版 MC"，它是一个专为研究 LLM 长期学习能力设计的 benchmark：

- **部分可观测**：ambiguous 字段模拟真实世界的信息不完全
- **多路径**：同一目标有多种达成方式，测试策略选择能力
- **条件依赖**：动作有前置条件，测试规划能力
- **状态转移确定性**：便于分析图谱质量
- **可配置复杂度**：通过调整维度数量和任务目标来控制难度

与已有的 crafting-only TextCraft / Plancraft 类环境相比，本环境额外强调挖矿、交易、探索和隐藏信息带来的路径选择问题；与真实 Minecraft 环境相比，本环境牺牲视觉和物理复杂度，保留可控的状态转移和可复现实验设置。

**普适性 — 框架可迁移到其他场景**

ExperienceGraph 框架的核心抽象（状态条件节点 + 动作边 + 统计 + 合并）不依赖于 Minecraft 语义。它可以迁移到：

| 应用场景 | 节点语义 | 边语义 | 适配方式 |
|---------|---------|--------|---------|
| **具身智能（机器人）** | 物理状态条件（位置、抓取状态） | 运动原语（抓取、移动、放置） | 状态 = 传感器读数的离散化 |
| **对话系统** | 对话状态（用户意图、已收集信息） | 对话动作（提问、确认、推荐） | 状态 = 槽位填充情况 |
| **代码生成** | 代码状态（已实现功能、测试通过情况） | 编码动作（写函数、重构、调试） | 状态 = 测试通过率 + 依赖关系 |
| **自动驾驶规划** | 驾驶场景条件（车道、障碍物、信号灯） | 驾驶原语（变道、加速、等待） | 状态 = 场景特征向量离散化 |

**关键洞察**：只要一个任务环境满足以下条件，ExperienceGraph 就可以适用：
1. 状态可以离散化为有限维度的条件集合
2. 存在多条路径到达同一目标
3. 同一动作在不同状态下效果不同
4. 智能体需要跨 episode 积累经验

论文的 Discussion 部分将展开讨论具身智能场景的迁移方案，作为 future work 的方向。

### 3.5 系统架构

```
┌─────────────────────────────────────────────────┐
│                  TextCraft 环境                   │
│  (状态管理 / 动作执行 / 奖励判定)                  │
└────────────────────┬────────────────────────────┘
                     │ 观察 + 奖励
                     ▼
┌─────────────────────────────────────────────────┐
│               Agent (LLM)                        │
│                                                  │
│  1. 接收当前状态                                  │
│  2. 生成一条新路径（探索）                         │
│  3. 从图谱中检索可达路径（利用）                    │
│  4. 综合成功率 + 逻辑判断，选择最优路径             │
│  5. 执行路径的下一步动作                           │
└────────────────────┬────────────────────────────┘
                     │ 执行结果
                     ▼
┌─────────────────────────────────────────────────┐
│          Graph Organizer (图管理器)               │
│                                                  │
│  - 将新路径整合进图谱                              │
│  - 基于状态相似度合并节点                          │
│  - 更新路径统计（成功/失败 +1）                    │
│  - 维护硬性条件的一致性                            │
└─────────────────────────────────────────────────┘
```

---

## 4. 实验设计

### 4.1 统一实验平台

所有智能体方法均在同一个 TextCraft 环境中实现，共享相同的状态表示、动作空间和游戏规则。每个智能体是一个独立的 Agent 类，接收相同格式的观测（observation），输出相同格式的动作（action），区别仅在于内部决策机制。

```
┌─────────────────────────────────────────────────────────┐
│                    TextCraft 环境                         │
│         (统一状态管理 / 动作执行 / 规则引擎)               │
└───────┬──────────┬──────────┬──────────┬───────────────┘
        │          │          │          │
        ▼          ▼          ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐ ┌────────────┐
   │ ReAct  │ │Reflexion│ │  Ours  │ │ Ablations  │
   │ Agent  │ │ Agent  │ │ Agent  │ │   Agent    │
   └────┬───┘ └────┬───┘ └────┬───┘ └─────┬──────┘
        │          │          │            │
        └──────────┴──────────┴────────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │  统一 Evaluation    │
              │  (指标计算 / 日志)   │
              └─────────────────────┘
```

### 4.2 实验条件（各智能体实现）

| 条件 | 方法 | 决策机制 | 记忆形式 |
|------|------|---------|---------|
| **Baseline** | ReAct Agent | 交替思考+行动，每轮独立 | 无跨 episode 记忆 |
| **Flat Memory** | Reflexion Agent | 行动后反思，记录经验文本 | 扁平文本列表，append-only |
| **Ours** | ExperienceGraph Agent | 探索新路径 + 对比已有路径 + 选择执行 | 结构化经验图谱 |
| **Ablation 1** | Graph w/o Explore | 仅从图谱检索路径，不生成新路径 | 经验图谱（冻结探索） |
| **Ablation 2** | Graph w/o Stats | 图谱不展示成功率统计 | 经验图谱（无统计信号） |

每个 Agent 实现相同的接口：
```python
class BaseAgent:
    def act(self, observation: GameState) -> Action:
        """根据当前观测返回下一步动作"""
        ...
    
    def update(self, trajectory: Trajectory, success: bool):
        """episode 结束后更新内部记忆/经验"""
        ...
```

### 4.3 实验参数

- **Episode 数量**：300 轮（每个条件）
- **目标任务**：3-5 个不同复杂度的任务
  - Easy: 合成铁套装（路径短，条件少）
  - Medium: 合成钻石套装（多路径，有 ambiguous 信息）
  - Hard: 合成附魔钻石套装（长路径，多前置依赖）
- **初始状态分布**：每轮随机生成初始状态（控制难度分布）
- **LLM**：GPT-4 / Claude（固定版本，所有 Agent 共享同一 LLM，控制变量）
- **最大步数**：每轮设定上限（如 30 步），超时视为失败

### 4.4 统一 Evaluation 框架

所有 Agent 的输出统一收集到 Evaluation 模块，计算相同指标。

**主要指标：**
1. **成功率（Success Rate）**：每 50 轮滑动窗口的任务完成率
2. **收敛速度（Convergence Speed）**：达到 80% 成功率所需的 episode 数
3. **步数效率（Step Efficiency）**：成功 episode 的平均动作步数

**次要指标：**
4. **图谱增长曲线**（仅 Ours）：节点/边数量随 episode 的变化
5. **路径多样性**（仅 Ours）：图谱中从初始状态到目标的独立路径数
6. **新路径采纳率**（仅 Ours）：探索生成的新路径被实际采用的比例
7. **Token 消耗**：各方法的 LLM 调用量对比（成本效率）
8. **图谱视图大小**（仅 Ours）：每次决策展示给 LLM 的 token 数，验证固定预算机制
9. **节点合并质量**（仅 Ours）：抽样检查合并决策的一致性与错误合并比例

**Evaluation 输出格式：**
```json
{
  "agent": "ExperienceGraph",
  "episode": 150,
  "task": "diamond_set",
  "success": true,
  "steps": 12,
  "total_tokens": 4500,
  "initial_state_hash": "abc123"
}
```

### 4.5 统计检验

- 每个条件重复 5 次（不同随机种子，但各 Agent 使用相同种子集合以保证可比性）
- 使用 paired t-test 或 Wilcoxon signed-rank test 比较条件间差异
- 报告 95% 置信区间和 effect size (Cohen's d)
- 绘制学习曲线（x 轴: episode, y 轴: 成功率）附 standard error band

---

## 5. 环境搭建计划

### 5.1 技术栈

- **环境**：Python，自定义 TextCraft 游戏引擎
- **图数据库**：NetworkX（轻量）或 Neo4j（如需持久化和复杂查询）
- **LLM 接口**：OpenAI API / Anthropic API
- **实验管理**：Weights & Biases 或简单 JSON 日志
- **可视化**：Matplotlib（学习曲线）+ Graphviz（图谱可视化）

### 5.2 开发阶段

| 阶段 | 内容 | 预计工时 |
|------|------|---------|
| Phase 1 | TextCraft 环境核心（状态、动作、规则） | 1-2 周 |
| Phase 2 | Agent prompting + ReAct baseline | 1 周 |
| Phase 3 | 图管理器（节点合并、统计更新） | 1-2 周 |
| Phase 4 | 完整 ExperienceGraph 流程 | 1 周 |
| Phase 5 | 实验运行 + 数据收集 | 2 周 |
| Phase 6 | 分析 + 论文撰写 | 2-3 周 |

---

## 6. 预期结果与可能结论

### 6.1 最佳情况（Strong Results）

- H1 验证：ExperienceGraph 在 300 轮后成功率达到 85%+，ReAct 停留在 40-50%
- H2 验证：ExperienceGraph 在第 100 轮达到 80% 成功率，Flat Memory 需要 200+ 轮
- H3 验证：Ablation 1 (无探索) 收敛到 70%，说明探索机制贡献了额外 15% 提升
- 学习曲线呈现清晰的对数增长模式

**结论**：结构化经验图谱显著优于扁平记忆和无记忆方法，图结构 + 统计信号 + 探索机制三者缺一不可。

### 6.2 中等情况（Moderate Results）

- 成功率提升显著但收敛速度差异不明显
- 探索机制在简单任务上贡献小，在复杂任务上贡献大

**结论**：经验图谱的优势随任务复杂度增加而凸显，对简单任务收益有限。可引导后续研究关注更复杂的任务场景。

### 6.3 负面情况（Weak/Null Results）

- 图谱增长带来的 prompt 长度增加反而干扰 LLM 决策
- 状态相似度合并引入错误，导致误导性经验

**应对**：分析失败模式，可能需要改进图谱压缩策略或状态表示方法。这本身也是有价值的 negative result。

---

## 7. 创新点总结（Contribution Statement）

1. **框架层面**：提出一种面向 LLM 智能体跨 episode 学习的条件路径图框架，实现结构化经验积累
2. **机制层面**：设计"先探索后对比"的决策流程，结合统计信号和 LLM 推理能力，实现探索-利用平衡
3. **实证层面**：在 TextCraft 环境中验证图结构经验积累相对于扁平记忆和无记忆方法的显著优势
4. **环境层面**：提出 TextCraft——一个适合研究 LLM 智能体长期学习能力的轻量级 benchmark

---

## 8. 潜在风险与缓解

| 风险 | 影响 | 缓解策略 |
|------|------|---------|
| LLM API 成本过高 | 300轮 × 5种子 × 5条件 = 高调用量 | 用 GPT-3.5 做开发调试，GPT-4 只跑最终实验 |
| 图谱膨胀导致 prompt 过长 | 超出 context window | 设计图谱摘要/剪枝机制，只展示 top-k 路径 |
| 文字环境过于简单 | Reviewer 质疑泛化性 | 讨论部分说明框架可迁移，TextCraft 只是验证平台 |
| 状态合并逻辑不准确 | 错误经验污染图谱 | 加入衰减机制，长期不成功的路径权重递减 |

---

## 9. 论文结构草案

1. **Introduction** — 问题定义 + 动机 + 贡献声明
2. **Related Work** — LLM agents, experience/memory mechanisms, graph-based planning
3. **Method** — TextCraft 环境 + ExperienceGraph 框架（Agent + Graph Organizer）
4. **Experiments** — 实验设置 + 主实验结果 + 消融实验
5. **Analysis** — 图谱演化可视化 + 案例分析 + 失败模式分析
6. **Discussion** — 局限性 + 泛化性讨论 + 与强化学习的联系
7. **Conclusion**

